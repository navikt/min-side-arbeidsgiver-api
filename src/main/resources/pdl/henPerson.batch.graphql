query($ident: ID!) {
    hentPersonBolk(identer: $ident) {
        ident,
        person {
            navn {
                fornavn
                mellomnavn
                etternavn
            }
        },
        code
    }
}